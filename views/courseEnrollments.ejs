<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= course.name %> Enrollment--Details
    </title>
   
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-blue-900 text-white">
    <a href="/educator">
        <div class="flex items-center justify-right">
            <img src="https://cdn-icons-png.flaticon.com/512/7518/7518214.png"
                class="w-35 h-12 ml-6 my-3 flex items-center justify-middle" alt="view course">

            
        </div>
    </a>
    <div class="container mx-auto my-8 p-8 max-w-1/2 bg-darkblue-800 rounded-lg shadow-md">
        <div>
            <h1 class="text-5xl font-semibold mb-4">
                <%= course.name %>: Enrollment Reports.
            </h1>
            <p class="mb-4 text-xl"><strong>Description:</strong>
                <%= course.description %>
            </p>
            <p class="mb-4 text-xl"><strong>Created By:</strong>
                <%= createdBy %>
            </p>
            <p class="mb-4 text-xl"><strong>Number of Enrollments:</strong>
                <%= enrollmentCnt %>
            </p>
        </div>
        <div class="bg-blue-700 ml-10 mb-4 p-8 rounded-lg shadow-md">
            <h2 class="text-2xl font-semibold mb-4">Enrollment Percentage of <%= course.name %>
            </h2>
           
            <div class="relative inline-block w-20 h-20">
                <div class="w-23 h-23 absolute inset-0 rounded-full bg-green-100 border-4 border-green-500">
                    <div class="flex items-center justify-center h-full ">
                        <span class="text-gary-400 font-bold text-xl">
                            <%= percentage %>%
                        </span>
                    </div>
                </div>
            </div>
        </div>
        <div class="flex items-center">
            <h2 class="text-xl font-semibold mb-2">Enrollments:</h2>
            <table class="w-fit ml-10 border-collapse border border-gray-300 text-darkblue-800">
                <thead>
                    <tr class="bg-black-200 p-10">
                        <th class="p-5">S.no</th>
                        <th class="p-5">Name of student</th>
                        <th class="p-5">Email</th>
                        <th class="p-5">Date of Enrollment</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(var i=0; i<enrolledstudents.length; i++) { %>
                        <tr class="<%= i % 2 === 0 ? 'bg-black-100' : 'bg-white' %>">
                            <td class="p-5">
                                <%= i + 1 %>
                            </td>
                            <td class="p-5">
                                <%= enrolledstudents[i].name %>
                            </td>
                            <td class="p-5">
                                <%= enrolledstudents[i].email %>
                            </td>
                            <td class="p-5">
                                <%= enrollments[i].createdAt.toDateString() %>
                            </td>
                        </tr>
                        <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>
